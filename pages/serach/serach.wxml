<view class="section">
  <input bindinput="bindInput" placeholder="搜索" focus="true" />
</view>
<view bindtouchstart="bindSearch" data-location="{{i.location}}" class="text_box" data-name="{{i.name}}" data-address="{{i.address}}" wx:for="{{tips}}" wx:for-item="i">
<view>{{i.name}}</view>
<view class='district'>{{i.district}}</view>
<view class='district'>{{i.location}}</view>
</view>